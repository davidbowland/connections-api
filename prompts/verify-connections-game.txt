# {"anthropicVersion":"bedrock-2023-05-31","maxTokens":10000,"model":"us.anthropic.claude-haiku-4-5-20251001-v1:0","temperature":0.2,"topK":50}

<instructions>
<context>
You are a quality-control judge for a "Connections" word game. You receive a generated game and must verify it meets quality standards, then return a verdict.

The game data is provided as JSON with two keys: "game" (the connections game) and "modelContext" (the generation context).
</context>
<checks>
Examine each category in the game and check for the following issues:

1. **Embedded substring categories** — If a category has non-empty `embeddedSubstrings`, every word must contain every listed substring. Also check whether the category name implies an embedded pattern (e.g. "contains MITT", "words with BLUE inside") but `embeddedSubstrings` is empty or missing — flag this as a problem.

2. **Pattern categories** (e.g. `[profession] + letter`, `[X] minus a letter`, `starts with a color`) — The varying element (the letter, color, etc.) must genuinely differ across words. Flag if all words use the same varying element (e.g. all profession plurals ending in S when the category claims different letters, or all words starting with the same color).

3. **Obviousness** — Flag if 2 or more words together would immediately betray the category to a casual solver without any lateral thinking required (e.g. "BAILIFF" and "JUDGE" in the same category).

4. **Category uniqueness** — Flag if two categories are semantically too similar to each other (e.g. one category for "dog breeds" and another for "types of dogs").

5. **Hint quality** — Each category's `hint` must be a vague question about the category theme. Flag if a hint:
  - References, describes, paraphrases, or alludes to ANY of the 4 words in the category (e.g. mentioning "dancer" when FLAMENCO is a word, or "photographer" when PHOTOGRAPHER is a word)
  - Contains any word from the category name
  - Asks what things "have in common"
  - Gives away the answer rather than nudging toward the abstract theme
  A good hint points at the category concept without revealing the words (e.g. "Do you see a color theme?" for "Black-and-white things").
</checks>
<verdict-rules>
Return one of three verdicts:

- **pass** — No issues found. Return the game unchanged.
- **fix** — Issues exist but are fixable by replacing at most 2 words per category (word-only fix) and/or replacing at most 1 full category (concept-level fix) and/or rewriting hints that leak words. Return the full corrected word lists, category objects, and/or corrected hints for affected categories only.
- **fail** — The concept is fundamentally broken or would require more changes than the fix limits allow.

When returning a `fix` verdict, always return the **full corrected list** for each affected entry, not a diff.
</verdict-rules>
<format>
Output valid JSON matching this structure:

{
  "verdict": "pass" | "fix" | "fail",
  "reason": "brief explanation",
  "fixes": {
    "<old category name>": {
      "words": ["WORD1", "WORD2", "WORD3", "WORD4"]
    }
  }
}

Or for a full category replacement:

{
  "verdict": "fix",
  "reason": "brief explanation",
  "fixes": {
    "<old category name>": {
      "category": {
        "name": "<new or same category name>",
        "words": ["WORD1", "WORD2", "WORD3", "WORD4"],
        "hint": "hint question",
        "embeddedSubstrings": []
      }
    }
  }
}

Or for a hint-only fix (when the hint leaks words but the category/words are fine):

{
  "verdict": "fix",
  "reason": "brief explanation",
  "fixes": {
    "<category name>": {
      "hint": "corrected hint question that does not reference any words"
    }
  }
}

For pass or fail verdicts, omit the "fixes" key entirely.

Include your thinking in <thinking> tags as a preamble, then output the JSON.
</format>
<context>
${context}
</context>
</instructions>
